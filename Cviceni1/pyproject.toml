[project]
name = "cviceni1-llm-tools"
version = "0.1.0"
description = "LLM tool-calling exercise"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.40.0",
    "python-dotenv>=1.0.1",
    "yfinance>=1.1.0",
]
